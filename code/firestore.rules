rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /portfolio/site {
      allow read: if true;
      allow write: if request.auth != null
        && request.auth.token.email == '1801327763@qq.com';
    }
  }
}
